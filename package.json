{
	"name": "filosign",
	"version": "1.0.0",
	"description": "Filosign monorepo",
	"private": true,
	"workspaces": [
		"test",
		"packages/client",
		"packages/server",
		"packages/contracts",
		"packages/lib/react-sdk",
		"packages/lib/crypto-utils",
		"packages/lib/shared"
	],
	"scripts": {
		"dev": "bun run --parallel client:dev server:dev",
		"client:dev": "bun run --cwd packages/client dev",
		"client:build": "bun run --cwd packages/client build",
		"client:start": "bun run --cwd packages/client start",
		"server:dev": "bun run --cwd packages/server dev",
		"server:start": "bun run --cwd packages/server start",
		"test": "bun run --cwd packages/* test",
		"lint": "bun run --cwd packages/* lint",
		"check": "bunx biome check --write",
		"clean": "rm -rf packages/*/dist packages/*/node_modules packages/*/.bun node_modules .bun",
		"prepare": "husky",
		"purge": "./scripts/purge.sh",
		"serloc": "./scripts/serloc.sh",
		"deploy": "./scripts/deploy.sh",
		"db:push": "bun run --cwd packages/server db:push"
	},
	"engines": {
		"bun": ">=1.0.0"
	},
	"packageManager": "bun@1.0.0",
	"devDependencies": {
		"@biomejs/biome": "2.3.14",
		"@types/sharp": "0.32.0",
		"husky": "9.1.7",
		"prettier": "3.8.1",
		"sharp": "0.34.5",
		"typescript": "5.9.3"
	},
	"dependencies": {
		"@hono/zod-validator": "0.7.6",
		"@privy-io/wagmi": "2.0.2",
		"dotenv": "17.2.4",
		"ipaddr.js": "2.2.0",
		"viem": "2.38.5"
	},
	"overrides": {
		"@tanstack/react-query": "^5.90.2"
	}
}