# Copy to `.env.local` in this folder (or set ASTRO_URL in repo-root `.env.local`).
# No VITE_/PUBLIC_ prefix — same key as apps/server (`ASTRO_URL`).
#
# With `bun dev -- --web`, astro serves assets on :3002:
ASTRO_URL=http://localhost:3002
