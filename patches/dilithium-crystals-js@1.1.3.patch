diff --git a/node_modules/dilithium-crystals-js/.bun-tag-1981a820dbd001b4 b/.bun-tag-1981a820dbd001b4
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/package.json b/package.json
index 263d5e9c932ff1dd723a2c3d88bc5f5d99e8bbf0..c0e97eb53429baf848baf266d74e296ed55860dc 100644
--- a/package.json
+++ b/package.json
@@ -2,7 +2,7 @@
   "name": "dilithium-crystals-js",
   "version": "1.1.3",
   "main": "src/node/index.js",
-  "browser": "dist/dilithium.min.js",
+  "browser": "src/browser/index.js",
   "files": [
     "src/**/*.js",
     "src/**/*.d.ts",
diff --git a/src/browser/index.js b/src/browser/index.js
index af3f14541de0ab15419bbffe1430b6209af37b84..98cca8ec773a81c7b5d9be143ae34be22329e0f1 100644
--- a/src/browser/index.js
+++ b/src/browser/index.js
@@ -1,35 +1,41 @@
 import dilithiumFactory from "../../dilithium.js";
-import { DILITHIUM_PARAMS, generateKeys, sign, verify } from "../common";
+import * as commonModule from "../common.js";
 
-// Function to fetch the WebAssembly file
-async function fetchWasm() {
-  const wasmUrl = chrome.runtime.getURL("dilithium.wasm");
-  console.log(`Fetching wasm from ${wasmUrl}`);
-  const response = await fetch(wasmUrl);
-  return await response.arrayBuffer();
-}
+const common = commonModule.default || commonModule;
+const { DILITHIUM_PARAMS, generateKeys, sign, verify } = common;
 
-// Create the Dilithium object
-const createDilithium = () => 
-  fetchWasm()
-    .then((wasmBinary) =>
-      dilithiumFactory({
-        wasmBinary,
-        locateFile: (path) => {
-          if (path.endsWith(".wasm")) {
-            return chrome.runtime.getURL("dilithium.wasm");
-          }
-          return path;
-        },
-      })
-    )
-    .then((dilithium) => ({
-      generateKeys: (kind, seed) => generateKeys(dilithium, kind, seed),
-      sign: (message, privateKey, kind) =>
-        sign(dilithium, message, privateKey, kind),
-      verify: (signature, message, publicKey, kind) =>
-        verify(dilithium, signature, message, publicKey, kind),
-      DILITHIUM_PARAMS,
-    }));
+/** @returns {string} Path to WASM binary */
+const getWasmUrl = () => {
+  const isExtension = typeof chrome !== "undefined" && chrome.runtime?.getURL;
+  return isExtension ? chrome.runtime.getURL("dilithium.wasm") : "/dilithium.wasm";
+};
 
-export { createDilithium };
\ No newline at end of file
+/** @returns {Promise<ArrayBuffer>} WASM binary */
+const fetchWasm = async () => {
+  const url = getWasmUrl();
+  console.log(`[dilithium] loading from ${url}`);
+  const res = await fetch(url);
+  return res.arrayBuffer();
+};
+
+/**
+ * Creates a Dilithium instance.
+ * @returns {Promise<Object>} The Dilithium API.
+ */
+export const createDilithium = async () => {
+  const wasmBinary = await fetchWasm();
+  const dilithium = await dilithiumFactory({
+    wasmBinary,
+    locateFile: (path) => (path.endsWith(".wasm") ? getWasmUrl() : path),
+  });
+
+  return {
+    generateKeys: (kind, seed) => generateKeys(dilithium, kind, seed),
+    sign: (msg, sk, kind) => sign(dilithium, msg, sk, kind),
+    verify: (sig, msg, pk, kind) => verify(dilithium, sig, msg, pk, kind),
+    DILITHIUM_PARAMS,
+  };
+};
+
+// High-quality HQ: Export a single-instance promise by default
+export default createDilithium();
diff --git a/src/node/index.js b/src/node/index.js
index a4f192eb2d607840810250073783106bd8ab3683..943fd2b77334799bfff5bfa35f3bd9c392158a77 100644
--- a/src/node/index.js
+++ b/src/node/index.js
@@ -1,42 +1,33 @@
-const fs = require("fs");
-const path = require("path");
+const fs = require("node:fs");
+const path = require("node:path");
 const dilithiumFactory = require("../../dilithium.js");
 const { DILITHIUM_PARAMS, generateKeys, sign, verify } = require("../common");
 
-// Read the WebAssembly file with fallback paths
-let wasmBinary;
-let wasmPath;
+/** @returns {[Buffer|null, Error|null]} Result pattern for WASM loading */
+const loadWasm = () => {
+  const binaryPath = [
+    path.join(__dirname, "../../dilithium.wasm"),
+    path.join(process.env.RESOURCES_PATH || process.cwd(), "assets/dilithium.wasm"),
+    path.join(process.cwd(), "dilithium.wasm")
+  ].find(p => fs.existsSync(p));
 
-try {
-  // First try the regular Node.js path
-  wasmPath = path.join(__dirname, "../../dilithium.wasm");
-  wasmBinary = fs.readFileSync(wasmPath);
-} catch (err) {
-  // If that fails, try the Electron production path
-  wasmPath = path.join(process.resourcesPath, "assets", "dilithium.wasm");
-  wasmBinary = fs.readFileSync(wasmPath);
-}
-
-// Define a custom locateFile function for Node.js
-const nodeLocateFile = (filename) => {
-  if (filename.endsWith(".wasm")) {
-    return wasmPath;
-  }
-  return filename;
+  return binaryPath 
+    ? [fs.readFileSync(binaryPath), null] 
+    : [null, new Error("[dilithium] binary not found")];
 };
 
-// Initialize the Dilithium module with the WebAssembly binary
+const [wasmBinary, err] = loadWasm();
+if (err) throw err;
+
 const dilithiumModule = dilithiumFactory({
   wasmBinary,
-  locateFile: nodeLocateFile,
+  locateFile: (f) => f.endsWith(".wasm") ? "dilithium.wasm" : f,
 });
 
 module.exports = dilithiumModule.then((dilithium) => ({
   generateKeys: (kind, seed) => generateKeys(dilithium, kind, seed),
-  sign: (message, privateKey, kind) =>
-    sign(dilithium, message, privateKey, kind),
-  verify: (signature, message, publicKey, kind) =>
-    verify(dilithium, signature, message, publicKey, kind),
+  sign: (msg, sk, kind) => sign(dilithium, msg, sk, kind),
+  verify: (sig, msg, pk, kind) => verify(dilithium, sig, msg, pk, kind),
   DILITHIUM_PARAMS,
   HEAP8: dilithium.HEAP8,
 }));
